# MediaPipe Tasks API (PoseLandmarker + HandLandmarker). First run downloads models to ./models/
mediapipe>=0.10.14
numpy>=1.24
opencv-python
pillow>=10.0.0
requests
# JARVIS assistant deps (optional — only needed for --jarvis mode)
pyttsx3>=2.90
pyaudio>=0.2.13
